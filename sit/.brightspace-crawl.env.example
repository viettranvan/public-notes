# Copy → sit/.brightspace-crawl.env (gitignore — KHÔNG commit URL thật)
# Hoặc: export BRIGHTSPACE_SERVICE_TOKEN_URL=... trước khi chạy crawl
#
# Endpoint nội bộ trả JSON { access_token, expires_in } — chỉ dùng từ Node crawl.

BRIGHTSPACE_SERVICE_TOKEN_URL=
